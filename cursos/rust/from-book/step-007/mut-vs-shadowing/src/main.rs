fn main() {
    let spaces = "   ";
    let spaces = spaces.len();
    println!("{spaces} espaços em branco");

    let mut spaces = "   ";
    // A linha abaixo dá erro pq o tipo continua sendo string
    // spaces = spaces.len();
}
